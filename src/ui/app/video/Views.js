import React from 'react';
import styled from 'styled-components';

const Views = styled.div.attrs({
  className: 'video-views',
})`
  padding: 1rem;
  grid-area: "views";
  text-align: right;
`;

export default Views;
